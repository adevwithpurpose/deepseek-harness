# Role packages

Enforced specialist role policies and delegation components. The group starts with `dsh-tool-restriction`, a preset-scoped Consumer that removes tools from both model schemas and dispatch. The role policy Service Definition, configuration Provider, and role delegation Consumer are built as separate packages because their lifecycle and deployment concerns evolve independently.
